name: scenickazatva_app
description: App for javisko.sk and Slovak festivals held by NOC

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.42+42

repository: https://github.com/kodekameratene/impuls-app-flutter
issue_tracker: https://github.com/kodekameratene/impuls-app-flutter/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  table_calendar: ^3.1.0
  url_launcher: ^6.0.0
  flutter_html: ^3.0.0-beta.2
  lazy_load_scrollview: ^1.3.0
  cached_network_image: ^3.2.1
  firebase_core: ^4.8.0
  firebase_database: ^12.4.0
  firebase_auth: ^6.5.0
  firebase_messaging: ^16.2.1
  firebase_analytics: ^12.4.0
  firebase_storage: ^13.4.0
  firebase_app_check: ^0.4.6
  markdown: ^7.0.0
  firebase_cached_image: ^0.10.0
  pointer_interceptor: ^0.10.1+2
  go_router: ^17.2.3
  firebase_ui_auth: ^3.0.1
  app_links: ^6.4.1
  wordpress_client: ^9.3.0
  intl: any
  flutter_native_splash: any
  path_provider: any
  flutter_quill: ^11.5.0
  flutter_quill_delta_from_html: ^1.5.3
  vsc_quill_delta_to_html: ^1.0.5
  image_picker: ^1.2.1
  http_cache_hive_store: ^5.0.1
  hive_ce: ^2.11.0
  hive_ce_flutter: ^2.3.0
  dio_cache_interceptor: ^4.0.3
  flutter_local_notifications: ^22.3.0
  share_plus: ^13.3.0

dev_dependencies:
  build_runner: ^2.4.8
  flutter_launcher_icons: ^0.14.3
  hive_ce_generator: ^1.9.0
  integration_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg

  assets:
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

  fonts:
    - family: Space Grotesk
      fonts:
        - asset: fonts/SpaceGrotesk-VariableFont_wght.ttf
          weight: 400

# To create image just run: $ flutter pub run flutter_launcher_icons
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: 'assets/images/icon.png'
  adaptive_icon_background: "assets/images/icon-background.png"
  adaptive_icon_foreground: "assets/images/icon-foreground.png"

#  flutter pub run flutter_native_splash:create
flutter_native_splash:
  image: assets/images/splash.png
  color: "#CCA965"
